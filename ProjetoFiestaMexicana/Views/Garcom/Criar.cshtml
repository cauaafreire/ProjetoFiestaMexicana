@model ProjetoFiestaMexicana.Models.Garcom
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "Novo Garçom";
}

<div class="page-header">
    <h2>Novo Garçom</h2>
</div>


<div class="card">
    @if (!ViewData.ModelState.IsValid)
    {
        <div class="validation">
            @foreach (var e in ViewData.ModelState.Values.SelectMany(v => v.Errors))
            {
                <div>@e.ErrorMessage</div>
            }
        </div>
    }

    <form asp-action="Criar" method="post">
        @Html.AntiForgeryToken()

        <div class="form-grid">
            <div class="field">
                <label asp-for="Nome">Nome Completo</label>
                <input asp-for="Nome" class="input" required />
                <span asp-validation-for="Nome" class="text-danger"></span>
            </div>

            <div class="field">
                <label asp-for="Cpf">CPF</label>
                <input asp-for="Cpf" class="input" maxlength="14" required/>
                <small class="hint">Apenas números, 11 dígitos.</small>
                <span asp-validation-for="Cpf" class="text-danger" style="font-size:0.85rem;"></span>
            </div>

            <div class="field">
                <label asp-for="Turno">Turno</label>
                <select asp-for="Turno" asp-items="ViewBag.Turnos" class="input">
                    <option value="">(Selecione o Turno)</option>
                </select>
                <span asp-validation-for="Turno" class="text-danger"></span>
            </div>
        </div>

        <div class="form-actions">
            <button type="submit" class="btn btn-light">Salvar</button>
            <a asp-action="Index" class="btn btn-primary">Cancelar</a>
        </div>
    </form>
</div>

<style>
    .page-header h2 {
        text-align: center;
        color: #2d2d2d;
        margin-top: 30px;
        font-family: 'Segoe UI', sans-serif;
    }

    .card {
        background: #ffffff;
        max-width: 600px;
        margin: 20px auto 50px auto;
        padding: 35px;
        border-radius: 15px;
        box-shadow: 0 4px 20px rgba(0,0,0,0.1);
        font-family: 'Segoe UI', sans-serif;
    }

    .form-grid {
        display: flex;
        flex-direction: column;
        gap: 20px;
    }

    .field {
        display: flex;
        flex-direction: column;
        gap: 8px;
    }

        .field label {
            font-weight: 600;
            color: #2d2d2d;
            font-size: 14px;
        }

    .input {
        padding: 12px 15px;
        border: 1px solid #ddd;
        border-radius: 10px;
        font-size: 15px;
        background-color: #fff;
        width: 100%;
        box-sizing: border-box;
        transition: border-color 0.3s;
    }

        .input:focus {
            border-color: #ff5a5f;
            outline: none;
        }

    .hint {
        color: #888;
        font-size: 12px;
        margin-top: -4px;
    }

    .form-actions {
        margin-top: 35px;
        display: flex;
        gap: 15px;
        justify-content: center;
    }

    .btn {
        display: inline-block;
        padding: 12px 24px;
        border-radius: 10px;
        font-weight: 600;
        text-decoration: none !important;
        cursor: pointer;
        text-align: center;
        min-width: 150px;
        font-size: 15px;
        transition: all 0.2s;
        border: none;
    }

        .btn-light, .btn:not(.btn-primary) {
            background-color: #ff5a5f !important;
            color: #ffffff !important;
        }

    .btn-primary {
        background-color: #2d2d2d !important;
        color: #ffffff !important;
    }

    .btn:hover {
        opacity: 0.85;
        transform: translateY(-1px);
        box-shadow: 0 4px 10px rgba(0,0,0,0.2);
    }

    .validation {
        background-color: #ffeaea;
        color: #ef4444;
        padding: 15px;
        border-radius: 8px;
        margin-bottom: 20px;
        border: 1px solid #ef4444;
        font-size: 14px;
    }

    .text-danger {
        color: #ef4444;
        font-size: 13px;
    }
</style>


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        const cpfInput = document.getElementById('Cpf');

        cpfInput.addEventListener('input', function () {
            let v = this.value.replace(/\D/g, '').substring(0, 11);

            if (v.length > 9) {
                v = v.replace(/(\d{3})(\d{3})(\d{3})(\d{0,2})/, '$1.$2.$3-$4');
            } else if (v.length > 6) {
                v = v.replace(/(\d{3})(\d{3})(\d{0,3})/, '$1.$2.$3');
            } else if (v.length > 3) {
                v = v.replace(/(\d{3})(\d{0,3})/, '$1.$2');
            }

            this.value = v;
        });

        document.querySelector('form').addEventListener('submit', function (e) {
            cpfInput.value = cpfInput.value.replace(/\D/g, '');
        });
    </script>
}
